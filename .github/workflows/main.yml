name: Build and push
on: [push]

jobs:
  build: 
    runs-on: ubuntu-latest
  
    steps:
    - uses: actions/checkout@v2
    - name: stage 1 (run checkout action)
      env:
        DOCKER_USER: ${{secrets.DOCKER_USER}}
        DOCKER_PASSWORD: ${{secrets.DOCKER_PASSWORD}}
        TAG: "hellovue/test:1.0.0"
      run: |
        docker login -u $DOCKER_USER -p $DOCKER_PASSWORD
    - name: stage 2 (build the docker image)
      run: |
        docker build . --file Dockerfile --tag hellovue/test:1.0.0
    - name: stage 3 ( push to registry:)
      run: |
        docker push ${{secrets.DOCKER_USER}}/hellovue/test:1.0.0
